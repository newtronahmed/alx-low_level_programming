#include "main.h"

/**
 * print_rev -a function that prints a string, in reverse, 
 * followed by a new line.
 * @s: input parameter.
 *
 * Return: void
 */
void print_rev(char *s)
{
	int i, j;

	for (i = 0; i != '\0'; i++)
	{
		continue;
	}
	for (j = i; j < 0; j--)
		putchar(*(s + j);
	putchar('\n');
}
